buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 21
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "25.1.8937393"
        kotlinVersion = "1.8.0"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:7.3.0")
        classpath("com.facebook.react:react-native:0.66.4") // Update to the latest version
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.5.21") // Update to the latest version
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}
